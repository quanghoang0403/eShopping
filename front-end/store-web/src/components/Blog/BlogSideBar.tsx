import { parseISO, format } from 'date-fns'

interface IProps {
  date: string
  className: string
}

export default function BlogSideBar(props: IProps) {
  const { date, className } = props
  return <></>
}
